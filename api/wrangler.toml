name = "techblog-api"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# ⚠️ Replace the database_id below with your actual D1 database_id
[[d1_databases]]
binding = "DB"
database_name = "techblog-db"
database_id = "5a3dfe2f-2fb9-4cc2-bac5-6192df2d349b"  # ← run `wrangler d1 create techblog-db` and paste the uuid here

[vars]
# Allowed frontend origins (CORS)
# Supports comma-separated domains; supports * wildcard; no trailing /
ALLOWED_ORIGIN = "https://techblog-94z.pages.dev"  # ← your frontend domain (supports comma-separated, wildcard *)

# Set ADMIN_TOKEN via `wrangler secret put ADMIN_TOKEN`, do not put it here

[dev]
port = 8787
